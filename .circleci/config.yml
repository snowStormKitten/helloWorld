version: 2
jobs:
  build:
    docker:
      - image: golang:latest

    working_directory: /app
    steps:
      - checkout

      - run: go get -v -t -d ./...
      - run: go test -v ./...
deployment:
    dockerhub:
      branch: master
      commands:
        - $DOCKER_HUB_TRIGGER
